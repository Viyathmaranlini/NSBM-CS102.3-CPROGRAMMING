#include <stdio.h>
int main()
{
    // Declaring Variable
    char ch;
    // User Input
    printf("Enter your Character or Symbol:");
    scanf("%c", &ch);
    // Display Output
    printf("%d 's integer equivalent of %c ", ch, ch);
}